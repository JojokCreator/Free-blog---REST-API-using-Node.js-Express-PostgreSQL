{
  "name": "hackathon_rest-api",
  "version": "1.0.0",
  "description": "Your task is to build a REST API using Node.js, Express & PostgreSQL. It will be a chance to embed everything we've learned so far and practice breaking down problems into solvable chunks.",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "start": "node -r dotenv/config app.js",
    "dev": "nodemon -r dotenv/config app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:createAuthTable": "node -r dotenv/config ./db/scripts/createAuthTable.js",
    "db:createPostsTable": "node -r dotenv/config ./db/scripts/createPostsTable.js",
    "db:createUsersTable": "node -r dotenv/config ./db/scripts/createUsersTable.js",
    "db:populatePostsTable": "node -r dotenv/config ./db/scripts/populatePostsTable.js",
    "db:populateUsersTable": "node -r dotenv/config ./db/scripts/populateUsersTable.js",
    "db:dropPostsTable": "node -r dotenv/config ./db/scripts/dropPostsTable.js",
    "db:dropUsersTable": "node -r dotenv/config ./db/scripts/dropUsersTable.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cookie-parser": "^1.4.6",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.7.3"
  },
  "devDependencies": {
    "dotenv": "^16.0.1",
    "nodemon": "^2.0.16"
  }
}
